# Check-in Lists

Check-in Lists allow you to check your attendees in at the venue of your event.

Create a master check-in list for all attendees or create special lists just for particular ticket types.


## Attributes

<%= partial "includes/shared/attributes", locals: { attributes: data.checkin_lists.attributes } %>

## Get all Check-in Lists

<%= partial "includes/requests/get",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists"
  }
%>
<%= partial "includes/admin_api/3.0/checkin_lists/index.json" %>

Retrieves all `CheckinLists` for the given `Event`.

`<%= "GET #{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.


## Get a Check-in List

<%= partial "includes/requests/get",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists/:checkin_list_slug"
  }
%>
<%= partial "includes/admin_api/3.0/checkin_lists/show.json" %>

Retrieves a `CheckinList` for the given `Event`.

`GET <%= "#{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists/:checkin_list_slug" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.
* `checkin_list_slug` - The `slug` attribute of a `CheckinList`.


## Create a Check-in List

<%= partial "includes/requests/post",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists",
    parameters: {
      checkin_list: {
        title: "Bowling Attendee List"
      }
    }
  }
%>
<%= partial "includes/admin_api/3.0/checkin_lists/create.json" %>

Create a new `CheckinList` belonging to the given `Event`.

`POST <%= "#{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.

### Parameters

<%= partial "includes/shared/parameters", locals: { parameters: data.checkin_lists.attributes.reject { |name,attribute| attribute["read_only"] } } %>


## Update a Check-in List

<%= partial "includes/requests/patch",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists/:checkin_list_slug",
    parameters: {
      checkin_list: {
        title: "Conference Dinner List"
      }
    }
  }
%>
<%= partial "includes/admin_api/3.0/checkin_lists/update.json" %>

Update a `CheckinList` belonging to the given `Event`.

`PATCH <%= "#{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists/:checkin_list_slug" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.
* `checkin_list_slug` - The `slug` attribute of a `CheckinList`.

### Parameters

<%= partial "includes/shared/parameters", locals: { parameters: data.checkin_lists.attributes.reject { |name,attribute| attribute["read_only"] } } %>

## Delete a Check-in List

<%= partial "includes/requests/delete",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists/:checkin_list_slug",
  }
%>

Delete an `Activity` belonging to the given `Event`.

`DELETE <%= "#{config[:api_endpoint]}/:account_slug/:event_slug/checkin_lists/:checkin_list_slug" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.
* `checkin_list_slug` - The `slug` attribute of a `CheckinList`.
