# RSVP Lists

<%= beta_feature "RSVP" %>

Invite people to register and target specific releases. An `RSVP List` has many [Release Invitations](#rsvp-release-invitations)

## Attributes

<%= partial "includes/shared/attributes", locals: { attributes: data.rsvp_lists.attributes } %>


## Get all RSVP Lists

<%= partial "includes/requests/get",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists"
  }
%>
<%= partial "includes/admin_api/3.0/rsvp_lists/index.json" %>

Retrieves all `RSVP Lists` for the given `Event`.

`<%= "GET #{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.

## Get an RSVP List

<%= partial "includes/requests/get",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists/:rsvp_list_slug"
  }
%>
<%= partial "includes/admin_api/3.0/rsvp_lists/show.json" %>

Retrieves an `RSVP List` for the given `Event`.

`GET <%= "#{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists/:rsvp_list_slug" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.
* `rsvp_list_slug` - The `slug` attribute of an `RSVP List`.

## Create an RSVP List

<%= partial "includes/requests/post",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists",
    parameters: {
      rsvp_list: {
        title: "Introduction"
      }
    }
  }
%>
<%= partial "includes/admin_api/3.0/rsvp_lists/create.json" %>

Create a new `RSVP List` belonging to the given `Event`.

`POST <%= "#{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.

### Parameters

<%= partial "includes/shared/parameters",
  locals: {
    parameters: data.rsvp_lists.attributes.reject { |name, attribute| attribute["read_only"] }
  }
%>

## Update an RSVP List

<%= partial "includes/requests/patch",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists/:rsvp_list_slug",
    parameters: {
      rsvp_list: {
        title: "New Introduction"
      }
    }
  }
%>
<%= partial "includes/admin_api/3.0/rsvp_lists/update.json" %>

Update an `RSVP List` belonging to the given `Event`.

`PATCH <%= "#{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists/:rsvp_list_slug" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.
* `rsvp_list_slug` - The `slug` attribute of an `RSVP List`.

### Parameters

<%= partial "includes/shared/parameters",
  locals: {
    parameters: data.rsvp_lists.attributes.reject { |name, attribute| attribute["read_only"] }
  }
%>

## Delete an RSVP List

<%= partial "includes/requests/delete",
  locals: {
    url: "#{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists/:rsvp_list_slug",
  }
%>

Delete an `RSVP List` belonging to the given `Event`.

`DELETE <%= "#{config[:api_endpoint]}/:account_slug/:event_slug/rsvp_lists/:rsvp_list_slug" %>`

* `account_slug` - The `slug` attribute of an `Account`.
* `event_slug` - The `slug` attribute of an `Event`.
* `rsvp_list_slug` - The `slug` attribute of an `RSVP List`.
