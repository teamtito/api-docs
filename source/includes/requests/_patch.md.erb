```shell
curl --request PATCH \
  --url '<%= apply_api_subsititions(url) %>' \
  --header 'Authorization: Token token=<%= config[:api_token] %>' \
  --header 'Accept: application/json' \
  --header 'Content-type: application/json' \
  --data '<%= parameters.to_json %>'
```
