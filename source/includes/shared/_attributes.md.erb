Attribute | Type | Description
--------- | ---- | -----------
<%- attributes.sort_by { |name, attribute| name }.each do |name, attribute| -%>
<%= name %> | `<%= attribute["type"] %>` | <%= attribute["description"].presence || "&mdash;" %> <%= "*Readonly*" if attribute["read_only"] %>
<%- end -%>
